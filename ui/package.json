{
  "name": "ui",
  "version": "1.0.0",
  "description": "A new Flutter project.",
  "scripts": {
    "lint": "flutter analyze",
    "build": "flutter pub get && npm run build:test",
    "build:test": "flutter pub run build_runner build --delete-conflicting-outputs",
    "build:web": "flutter build web",
    "build:ios": "flutter build ios",
    "build:apk": "flutter build apk",
    "test": "flutter test -v",
    "test:web": "flutter test -v --web-renderer auto",
    "web": "flutter run -d chrome",
    "mobile": "flutter run -d emulator-5554"
  }
}
